#include <bits/stdc++.h>
using namespace std;
#define _ ios_base::sync_with_stdio(0); cin.tie(0);
typedef long long ll;
const int INF = 0x3f3f3f3f;
const ll LINF = 0x3f3f3f3f3f3f3f3fll;
#define endl '\n'
#define sz(x) (int)x.size()

void solve(){
    ll k; cin >> k;
    if(k == 1) cout << 2 << endl;
    else if(k == 2) cout << 3 << endl;
    else {
        ll cur = 3, val = 2;
        ll next = cur * 2 + 2, nval = ; 
        for(int i = )
    }
}   



int main(){ //_
    // int t; cin >> t;
    // while(t --) solve();
    solve();
    return 0;
} 
