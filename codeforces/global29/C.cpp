#include <bits/stdc++.h>
using namespace std;
#define _ ios_base::sync_with_stdio(0); cin.tie(0);
typedef long long ll;
const int INF = 0x3f3f3f3f;
const ll LINF = 0x3f3f3f3f3f3f3f3fll;
#define sz(x) (int)x.size()
#define int ll

void solve(){
    int n; cin >> n;
    string s; cin >> s;
    vector<vector<bool>> can(n, vector<bool>(2)); // para cada atribuo se consigo uma atribuição se o iesimo for l ou r
    for(int i = 0; i < n; i ++){
        if(s[i] == '0'){
            if()


        }
    }

}

int32_t main(){ _
    int n; cin >> n;
    while(n --) solve();
}

// oh yes
